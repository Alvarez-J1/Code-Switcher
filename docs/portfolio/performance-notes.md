# Performance Notes

The project is static and has a small runtime footprint.

External libraries are loaded from CDN for Prism.js and Font Awesome.

The GitPulse image uses lazy loading and async decoding so it does not compete with the initial hero content.
