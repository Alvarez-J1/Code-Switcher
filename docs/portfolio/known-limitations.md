# Known Limitations

The navigation links are placeholders and do not route to separate pages.

The authentication buttons are visual controls only.

The code samples are static examples and do not connect to a live API from the page.
