# JavaScript Structure

The JavaScript is split between interaction logic and decorative star generation.

`script.js` handles the copy action, language tab switching, Prism re-highlighting, and footer observer.

`stars.js` appends decorative star elements after the DOM is ready.
