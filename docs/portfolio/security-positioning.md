# Security Positioning

The landing page positions Code Switcher as a developer authentication product.

The copy emphasizes secure API access, SDKs, and a public OpenAPI specification.

The current project is a static front-end demo, so the security messaging is conceptual rather than backed by live authentication code.
