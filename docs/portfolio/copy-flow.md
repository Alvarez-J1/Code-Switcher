# Copy Flow

The copy control reads the active code snippet and writes it to the clipboard.

The implementation uses the browser Clipboard API and reports failure in the console if copying is blocked.

The behavior is intentionally small and direct for a static portfolio landing page.
