# Mobile QA

Primary mobile checks should include the fixed header, hamburger icon, hero title spacing, auth buttons, and code section width.

The main title should not collide with the header on narrow screens.

The code tabs should remain horizontally accessible without breaking the page.
