# Favicon Notes

The favicon is linked from `git.png` in the document head.

The image uses transparency around the mark so the browser tab does not show a heavy black square.

This is a small polish detail that helps the project feel more finished in a portfolio context.
