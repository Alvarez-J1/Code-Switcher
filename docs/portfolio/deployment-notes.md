# Deployment Notes

The project can be deployed as a static site because it does not require a backend build step.

Hosting platforms such as Vercel, Netlify, GitHub Pages, or any static file host can serve the current files.

After deployment, the main checks are asset paths, CDN loading, and favicon rendering.
