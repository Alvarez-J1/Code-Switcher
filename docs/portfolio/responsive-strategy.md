# Responsive Strategy

The layout uses a mobile-first header pattern with a hamburger control at small widths.

On larger screens, the navigation expands into a horizontal menu and the auth actions remain visible.

The hero title receives mobile-specific spacing so it has breathing room below the fixed header.
