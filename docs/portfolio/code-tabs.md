# Code Tabs

The language tabs use `data-language` attributes to map each button to its matching code block.

Only one tab and one code block are active at a time.

This keeps the JavaScript simple while still demonstrating interactive UI behavior.
