# Navigation Notes

The header keeps brand, navigation links, and auth actions grouped in predictable areas.

The mobile menu toggles the navigation visibility with a small JavaScript listener.

The navigation also includes an accessible label so assistive technologies can identify it as the primary navigation region.
